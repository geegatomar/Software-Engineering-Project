<%- include('../../partials/header') %>
    <div class="wrap">
        <div class="jumbotron text-center">
            <div class="container">
                <i class="fas fa-key fa-6x"></i>
                <h1 class="display-3">My Applied Jobs</h1>

                <%jobs.forEach((job) => {%>
                    <hr>
                    <!-- <div class="form-group">
                <p>Organization</p>
                <p> <%= job.organization %> </p>
                <p>Status</p>
                <p> <%= job.status %> </p>    
            </div> -->

                    <div class="row">
                        <div class="col-sm-6">
                            <div class="card">
                                <div class="card-body">
                                    <h5 class="card-title">
                                        <%= job.organization %>
                                    </h5>
                                    <hr>
                                    <p class="card-text"> <b>Status: </b>
                                        <%= job.status %>
                                    </p>
                                    <form class="choose" method="POST" action="/view_test/<%=job.id%>">
                                        <button type="submit" class="btn btn-info">View test</button>
                                    </form>
                                    <form class="choose" method="POST" action="/view_codingtest/<%=job.id%>">
                                        <button type="submit" class="btn btn-info">View Coding test</button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    </div>
                    <br>
                    <%})%>
                        <hr>
                        <a class="btn btn-light btn-lg" href="/home" role="button">Back To Home</a>
            </div>
        </div>
    </div>
    <%- include('../../partials/footer') %>